To compile the code you need to write:
	gcc main.c Cell.c -I include -L.  -lMaze -o maze -lm
After you will get the executable file with name maze
Also there is executable maze file in archive which works properly
