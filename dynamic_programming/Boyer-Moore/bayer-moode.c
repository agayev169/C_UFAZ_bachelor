#include <stdio.h>

// Declarations
int find_in_word(char *to_find, char *word);


// Definitions
int find_in_word(char *to_find, char *word) {
    char *shift = (char*) calloc(len(to_find), sizeof(char));
}